services:
  app:
    image: node:22
    ports:
      - "3012:3012"
    environment:
      - PORT=3012
      - PASSWORD=neopin123
    volumes:
      - .:/app
    working_dir: /app
    command: ["node", "server.js"]