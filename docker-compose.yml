version: "3.8"

services:
  app:
    image: node:22
    ports:
      - "3012:3012"
    environment:
      - PORT=3012
      - PASSWORD=neopin123
    volumes:
      - .:/home/docker/neopin:rw
    working_dir: /home/docker/neopin
    command: sh -c "npm install && node server.js"
