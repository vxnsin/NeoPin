services:
  app:
    image: node:22
    command: sh -c "npm install && node server.js"
    working_dir: /app
    ports:
      - "3012:3012"
    volumes:
      - ./:/app
    environment:
      - PORT=3012
      - PASSWORD=neopin123