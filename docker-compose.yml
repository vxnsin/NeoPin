services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3012:3012"
    environment:
      - PORT=3012
      - PASSWORD=neopin123
    volumes:
      - .:/app
    command: ["node", "server.js"]
